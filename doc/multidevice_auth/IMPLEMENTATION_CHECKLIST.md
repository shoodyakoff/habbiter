# Multi-Device Auth Fix - Чек-лист реализации

**Дата:** 2026-01-13
**Статус:** Ready to implement

---

## 🎯 Цель

Исправить проблему разлогинивания пользователя при входе с другого устройства/браузера.

---

## ⚡ Quick Start (5 минут)

### Шаг 1: Проверьте настройки Supabase

1. Откройте: `https://supabase.com/dashboard/project/<your-project-id>/auth/sessions`
2. Убедитесь, что **"Single session per user" = OFF (выключено)**
3. Если включено - выключите и сохраните

### Шаг 2: Запустите SQL проверку

```sql
-- Проверить текущее количество сессий
SELECT
  u.email,
  COUNT(s.id) as active_sessions
FROM auth.users u
LEFT JOIN auth.sessions s ON s.user_id = u.id
WHERE u.email LIKE '%@telegram.user'
GROUP BY u.id, u.email
HAVING COUNT(s.id) > 0
ORDER BY active_sessions DESC;
```

### Шаг 3: Начните рефакторинг

Следуйте порядку ниже (не меняйте последовательность!).

---

## 📋 Checklist реализации

### Подготовка

- [ ] Проверены настройки Supabase (Single session = OFF)
- [ ] Выполнен SQL запрос для baseline метрик
- [ ] Создана feature ветка: `git checkout -b fix/multidevice-auth`

---

### Файл 1: `telegram-auth/index.ts` ⭐ ПРИОРИТЕТ 1

#### Изменения:

**1. Заменить строки 64-72 (получение userId)**
- [ ] Удалить блок с `if (!userId) { signInWithPassword }`
- [ ] Добавить поиск через `supabase.from('users').select('id').eq('telegram_id', data.id).single()`
- [ ] Добавить fallback для рассинхрона (см. ТЗ, строки 260-286)

**2. Заменить строки 96-98 (updateUserById)**
- [ ] Получить текущего пользователя: `getUserById(userId)`
- [ ] Добавить условие: `if (user.app_metadata?.is_subscribed !== isSubscribed)`
- [ ] Обновлять только если изменилось

**3. Проверить строки 101-104**
- [ ] Убедиться, что остался ТОЛЬКО ОДИН вызов `signInWithPassword`

#### Тестирование:
- [ ] Локально запустить функцию через `supabase functions serve telegram-auth`
- [ ] Протестировать авторизацию
- [ ] Проверить, что создается только 1 сессия (SQL запрос)
- [ ] Коммит: `git commit -m "fix(auth): optimize telegram-auth to support multiple sessions"`

---

### Файл 2: `telegram-auth-miniapp/index.ts` ⭐ ПРИОРИТЕТ 2

#### Изменения:

**Аналогичные изменения как в Файл 1:**
- [ ] Заменить строки 62-70 на поиск через `public.users`
- [ ] Добавить условие перед `updateUserById` (строка 94)
- [ ] Проверить финальный `signInWithPassword` (только один)

#### Тестирование:
- [ ] Тест через Telegram Mini App
- [ ] Проверить количество сессий
- [ ] Коммит: `git commit -m "fix(auth): optimize telegram-auth-miniapp for multiple sessions"`

---

### Файл 3: `telegram-webhook/index.ts` - функция `authorizeUser` ⭐ ПРИОРИТЕТ 3

#### Изменения:

**1. Заменить строки 164-175**
- [ ] Поиск через `public.users` по `telegram_id`
- [ ] Создание через `createUser` только если не найден
- [ ] Fallback для рассинхрона (НЕ через signIn, а через `admin.listUsers`)

**2. УДАЛИТЬ строки 173-175**
- [ ] Полностью убрать блок с `signInWithPassword` (он не нужен в webhook!)

**3. Заменить строки 189-192**
- [ ] Добавить условное обновление метаданных
- [ ] Обновлять только если `is_subscribed` изменился

#### Тестирование:
- [ ] Протестировать flow `/start auth_<token>` в Telegram боте
- [ ] Убедиться, что webhook не создает лишних сессий
- [ ] Коммит: `git commit -m "fix(auth): remove unnecessary signIn from webhook"`

---

### Файл 4: `generate-auth-token/index.ts` ⭐ ПРИОРИТЕТ 4

#### Проверка:
- [ ] Убедиться, что `signInWithPassword` вызывается ТОЛЬКО в блоке `action === 'poll'` и `status === 'success'`
- [ ] Убедиться, что НЕТ вызовов `updateUserById`
- [ ] Изменений НЕ требуется ✅

---

## 🧪 Финальное тестирование

### Тест 1: Multi-device auth (основной!)

- [ ] Открыть приложение в Chrome на ПК
- [ ] Авторизоваться
- [ ] Открыть приложение в Safari на телефоне
- [ ] Авторизоваться
- [ ] Вернуться в Chrome и обновить страницу
- [ ] **Результат:** Остался авторизованным ✅

### Тест 2: Количество сессий

```sql
-- Посчитать сессии после входа
SELECT COUNT(*) FROM auth.sessions WHERE user_id = '<test-user-uuid>';
```

- [ ] **Результат:** Создается только 1 новая сессия при входе

### Тест 3: Метаданные не обновляются без причины

```sql
-- Записать updated_at ПЕРЕД повторным входом
SELECT email, updated_at FROM auth.users WHERE email = '<test-email>';
-- [Авторизоваться повторно с тем же статусом подписки]
-- Проверить updated_at ПОСЛЕ
SELECT email, updated_at FROM auth.users WHERE email = '<test-email>';
```

- [ ] **Результат:** `updated_at` НЕ изменился (если подписка не менялась)

---

## 🚀 Деплой

### Production deployment

```bash
# 1. Залить изменения
git push origin fix/multidevice-auth

# 2. Создать PR
gh pr create --title "fix: support multi-device authentication" \
  --body "Fixes #<issue-number>. Optimizes auth flow to prevent session invalidation across devices."

# 3. После мержа - задеплоить функции
supabase functions deploy telegram-auth
supabase functions deploy telegram-auth-miniapp
supabase functions deploy telegram-webhook
# generate-auth-token не трогаем (изменений нет)
```

---

## 📊 Мониторинг после деплоя

### Через 1 день:
- [ ] Проверить логи Edge Functions на ошибки
- [ ] Опросить 2-3 пользователей о проблемах с входом

### Через 1 неделю:
- [ ] Запустить SQL метрики из [Technical_Specification.md](Technical_Specification.md#метрики-успеха-после-деплоя)
- [ ] Количество жалоб = 0
- [ ] Средний lifetime сессий вырос
- [ ] Среднее количество сессий на пользователя = 1.5-2

---

## 🆘 Откат (если что-то сломалось)

### Быстрый откат:
```bash
# Откатить одну функцию
supabase functions deploy telegram-auth --project-ref <ref> --no-verify-jwt
# Или через git
git revert <commit-hash>
git push
```

---

## 📚 Документация

- [Business_Requirements.md](Business_Requirements.md) - Бизнес-требования
- [Design.md](Design.md) - Архитектурное решение
- [Technical_Specification.md](Technical_Specification.md) - Полное ТЗ с кодом

---

## ✅ Definition of Done

Задача считается выполненной когда:

1. ✅ Все 4 файла рефакторены по чек-листу
2. ✅ Все тесты пройдены успешно
3. ✅ Код задеплоен в production
4. ✅ Через неделю: 0 жалоб на разлогинивание
5. ✅ Метрики показывают наличие множественных сессий у пользователей

---

**Время на реализацию:** 2-4 часа
**Риск:** Низкий (минимальные изменения, четкий rollback plan)
