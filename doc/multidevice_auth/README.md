# Multi-Device Authentication Fix

## 📖 Описание проблемы

**Симптом:** Пользователь авторизуется на телефоне, затем на компьютере - при попытке открыть приложение на телефоне его разлогинивает.

**Причина:** При каждом входе создается 2+ сессии из-за множественных вызовов `signInWithPassword` + агрессивные обновления `updateUserById` могут провоцировать инвалидацию refresh tokens.

**Решение:** Оптимизация потока авторизации в Edge Functions для минимизации побочных эффектов.

---

## 🗂️ Структура документации

### Для быстрого старта:
📄 **[IMPLEMENTATION_CHECKLIST.md](IMPLEMENTATION_CHECKLIST.md)** ⭐ START HERE
→ Пошаговый чек-лист реализации (5 мин настройка + 2-4 часа на код)

### Детальная документация:

1. 📋 **[Business_Requirements.md](Business_Requirements.md)**
   - Jobs To Be Done
   - Бизнес-метрики успеха
   - Пользовательские сценарии

2. 🏗️ **[Design.md](Design.md)**
   - Архитектурное решение (Вариант А vs Б)
   - Анализ текущей проблемы
   - Trade-offs и риски

3. ⚙️ **[Technical_Specification.md](Technical_Specification.md)**
   - Полное ТЗ с примерами кода
   - Детали реализации по файлам
   - План тестирования
   - Метрики успеха

---

## 🚀 Quick Start

### 1. Проверьте настройки Supabase (1 минута)

```
Dashboard → Authentication → Sessions
```

✅ **"Single session per user" должна быть ВЫКЛЮЧЕНА**

### 2. Запустите baseline метрики

```sql
SELECT
  u.email,
  COUNT(s.id) as active_sessions
FROM auth.users u
LEFT JOIN auth.sessions s ON s.user_id = u.id
WHERE u.email LIKE '%@telegram.user'
GROUP BY u.id, u.email
ORDER BY active_sessions DESC;
```

### 3. Следуйте чек-листу

Откройте [IMPLEMENTATION_CHECKLIST.md](IMPLEMENTATION_CHECKLIST.md) и следуйте пошаговой инструкции.

---

## 🎯 Ключевые изменения

### Было (проблема):
```typescript
// 1. Создаем пользователя
createUser()

// 2. Если существует - входим (Сессия #1 - мусор)
if (!userId) {
  signInWithPassword() // ❌ Лишняя сессия
}

// 3. Обновляем метаданные ВСЕГДА (даже если не изменилось)
updateUserById() // ❌ Может инвалидировать токены

// 4. Входим снова (Сессия #2)
signInWithPassword() // ✅ Эта нужна
```

**Проблема:**
- 2 вызова `signInWithPassword` = 2 новые сессии
- Безусловный `updateUserById` может инвалидировать refresh tokens
- Итог: старые сессии на других устройствах ломаются

### Стало (решение):
```typescript
// 1. Ищем в таблице (НЕ через Auth API)
const user = await supabase.from('users').select('id').eq('telegram_id', id).single()

// 2. Создаем только если не найден
if (!user) {
  createUser()
}

// 3. Обновляем ТОЛЬКО если изменилось
const currentUser = await getUserById(userId)
if (currentUser.app_metadata.is_subscribed !== newValue) {
  updateUserById() // ✅ Условное обновление
}

// 4. Входим ОДИН РАЗ
signInWithPassword() // ✅ Единственный вызов
```

**Улучшения:**
- ✅ Только 1 вызов `signInWithPassword` = 1 новая сессия
- ✅ Условное обновление метаданных (только при реальном изменении)
- ✅ Старые сессии остаются активными

---

## 🔄 Визуальное сравнение flow

### ❌ БЫЛО (создает 2 сессии + ломает старые)

```
User → Edge Function → Auth: createUser
                     ↓
                     Auth: signInWithPassword (получить ID)
                     ↓
                     ❌ Session #1 создана (мусор)
                     ↓
                     DB: upsert public.users
                     ↓
                     Auth: updateUserById (всегда!)
                     ↓
                     ⚠️  Может инвалидировать старые refresh tokens
                     ↓
                     Auth: signInWithPassword (финальная)
                     ↓
                     ✅ Session #2 создана (возвращаем клиенту)
```

**Итог:** 2 новые сессии, старые токены могут быть инвалидированы

---

### ✅ СТАЛО (создает 1 сессию, не трогает старые)

```
User → Edge Function → DB: SELECT from public.users
                     ↓
                     (userId найден через таблицу, не через Auth)
                     ↓
                     DB: upsert public.users
                     ↓
                     Auth: getUserById (проверить метаданные)
                     ↓
           ┌─────────┴─────────┐
           │ is_subscribed     │
           │ изменился?        │
           └─────────┬─────────┘
               Нет   │   Да
                 ┌───┴───┐
                 │       ▼
                 │   Auth: updateUserById
                 │       │
                 └───┬───┘
                     ▼
                     Auth: signInWithPassword (единственный!)
                     ↓
                     ✅ Session #1 создана (возвращаем клиенту)
```

**Итог:** 1 новая сессия, старые токены не трогаем

---

## 📊 Expected Results

### До фикса:
- ❌ 2 сессии создается при каждом входе
- ❌ Разлогинивание при входе с другого устройства
- ❌ Множественные ненужные обновления в auth.users

### После фикса:
- ✅ 1 сессия создается при входе
- ✅ Работа с нескольких устройств одновременно
- ✅ Метаданные обновляются только при реальном изменении

---

## 🧪 Как протестировать

1. **Откройте приложение на компьютере** (Chrome)
2. **Авторизуйтесь**
3. **Откройте на телефоне** (Safari), не закрывая первую вкладку
4. **Авторизуйтесь** на телефоне
5. **Вернитесь на компьютер** и обновите страницу

**Ожидаемый результат:** Остались авторизованными на обоих устройствах ✅

---

## 🔧 Файлы для изменения (в порядке приоритета)

1. ⭐ `supabase/functions/telegram-auth/index.ts` (ПРИОРИТЕТ 1)
2. ⭐ `supabase/functions/telegram-auth-miniapp/index.ts` (ПРИОРИТЕТ 2)
3. `supabase/functions/telegram-webhook/index.ts` - функция `authorizeUser` (ПРИОРИТЕТ 3)
4. `supabase/functions/generate-auth-token/index.ts` - только проверка (ПРИОРИТЕТ 4)

**Стратегия:** Рефакторить по одному файлу, тестировать, коммитить.

---

## 📚 Полезные ресурсы

### Документация Supabase:
- [User Sessions](https://supabase.com/docs/guides/auth/sessions)
- [Auth Architecture](https://supabase.com/docs/guides/auth/architecture)
- [Server-side Auth](https://supabase.com/docs/guides/auth/server-side/advanced-guide)

### Внутренние документы:
- [Project Context](../Project_Context.md) - общий контекст проекта

---

## ⏱️ Timeline

- **Подготовка:** 5-10 минут (проверка настроек Supabase)
- **Реализация:** 2-4 часа (рефакторинг 4 файлов)
- **Тестирование:** 30 минут
- **Деплой:** 15 минут
- **Мониторинг:** 1 неделя после деплоя

**Total:** ~1 день работы + неделя мониторинга

---

## 🆘 Что делать если сломалось

### Быстрый откат:
```bash
# Откатить через git
git revert <commit-hash>
git push

# Или откатить одну функцию
supabase functions deploy telegram-auth --project-ref <ref>
```

Подробнее в [Technical_Specification.md → Откат изменений](Technical_Specification.md#откат-изменений-rollback-plan)

---

## ✅ Definition of Done

- [ ] Все 4 файла отрефакторены
- [ ] Локальные тесты пройдены
- [ ] Деплой в production
- [ ] 0 жалоб на разлогинивание через неделю
- [ ] Метрики показывают множественные активные сессии

---

## 👥 Contacts

**Автор ТЗ:** Trae AI
**Дата создания:** 2026-01-13
**Статус:** Ready to implement
